def sort_list(lst):
  """Sorts a list in ascending order.

  Args:
    lst: The list to be sorted.

  Returns:
    A new list containing all items from lst in ascending order.
  """
  return sorted(lst)